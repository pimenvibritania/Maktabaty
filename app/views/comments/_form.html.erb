<%= form_for([@book,@book.comments.build]) do |f| %>
	<p>
		<%= current_user.email %>
	</p>
	<p>
		<%= f.label :body %> <br>
		<%= f.text_area :body %>
	</p>
	<br>
	<p>
		
		<%= f.submit %>
	</p>

	<% end %>
